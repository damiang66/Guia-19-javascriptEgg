//import Swal from "sweetalert2";
let valores = [true, 5, false, "hola", "adios", 2];
let mayor="";
let a;
let tipo=""
for (let index = 0; index < valores.length; index++) {
  tipo= valores[index];
  if (tipo === 'boolean'){
    console.log(tipo);
  }
  
  /*
  let tipo = typeof(valores[index]);
  if(tipo=='string'){
    if (valores[index]>mayor){
      mayor=valores[index];
      console.log('desde el for '+ mayor);
    }
  }
  if(tipo=='boolean'){
    console.log(valores[index]);
  }
  
  
  */

}


//Swal.fire("Resultado", `el perimetro es  ${Circulo.perimetro}  el area es ${Circulo.area}`,'success')

















//
